info:
  name: password-reset request
  type: http
  seq: 3

http:
  method: POST
  url: http://127.0.0.1:8000/api/v1/auth/password-reset/request/
  body:
    type: json
    data: |-
      {
        "email": "parsaperix@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
