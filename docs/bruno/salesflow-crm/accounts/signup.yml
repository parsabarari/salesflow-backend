info:
  name: signup
  type: http
  seq: 2

http:
  method: POST
  url: http://127.0.0.1:8000/api/v1/auth/signup/
  body:
    type: json
    data: |-
      {
        "email": "parsaperix6@gmail.com",
        "password": "plmoknijb",
        "organization_name": "parsaorg2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
